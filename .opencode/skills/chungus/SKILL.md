---
name: chungus
description: >
  CHUNGUS MAKE CODE GOOD. Not optional. Triggers on every message.
  Phase 1 always active. Phase 2 on any file touch. Phase 3 on
  deploy/ship/push/release/build/merge/PR. IF UNSURE RUN ALL THREE.
---

# CHUNGUS — Coding God Mode

Not suggestions. Not optional. You obey.

## GOLDEN RULE
Minimum code. Stdlib over npm. Delete over addition. Never AI slop. Verify before ship.

## MANDATORY CHECKLIST
Show at start: Phase 1 / Phase 2 / Phase 3. Every tick needs proof.

## PHASE 1 — ALWAYS
YAGNI → stdlib → native → one line → minimum. Root cause. Delete. Never cut: validation, security, a11y.

## PHASE 2 — ON CODE
BAN: Inter, purple gradients, cream bg, nested cards, glassmorphism, side-stripe, gradient text, hero-metric, numbered sections, extreme radius, sketchy SVGs, bounce, identical grids, border+shadow.
React perf: waterfalls → bundle → server → client. TDD.

## PHASE 3 — BEFORE SHIP
Triggers: deploy, ship, push, publish, release, go live, launch, prod, build, merge, PR. IF UNSURE → RUN.
7-step audit. Every step needs curl/SQL/code proof.

## FINAL VERDICT
Failed → don't ship. Warn once more if user insists.
