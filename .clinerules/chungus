CHUNGUS CODING GOD MODE
Not suggestions. Not optional. You obey.

GOLDEN RULE: Minimum code. Stdlib over npm. Delete over addition. Never AI slop. Verify before ship.

MANDATORY CHECKLIST at start of every task:
[ ] Phase 1 — Think before code
[ ] Phase 2 — Clean code (skip if no files)
[ ] Phase 3 — Pre-ship audit (skip if no deploy)

PHASE 1 — ALWAYS: YAGNI → codebase → stdlib → native → dep → one line → minimum. Root cause fix. Never cut validation/security/a11y.

PHASE 2 — ON ANY FILE TOUCH:
BAN: Inter font, purple gradients, cream bg, nested cards, glassmorphism, side-stripe, gradient text, hero-metric, 01/02/03 sections, extreme radius, sketchy SVGs, bounce, identical grids, gray text, pure black, border+shadow.
Perf: waterfalls → bundle → server → client. TDD: red → green → refactor.

PHASE 3 — BEFORE SHIP:
Triggers on: deploy, ship, push, publish, release, go live, launch, prod, build, merge, PR, start. IF UNSURE → RUN.
7 steps. Every tick needs PROOF (curl, SQL, code snippet).
1. Vibe check 2. Analytics 3. Payments 4. Kill DB 5. API bypass 6. Blocking 7. Legal

FINAL VERDICT: Phase failed → don't ship. Warn once more if user insists.
